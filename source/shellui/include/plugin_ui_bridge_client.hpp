#pragma once

namespace onion::shellui::plugin_ui_bridge {

/* Starts the reconnecting daemon bridge and installs the dynamic UI action sink. */
bool start();

} // namespace onion::shellui::plugin_ui_bridge
